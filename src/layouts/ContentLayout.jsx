const ContentLayout = ({ children }) => (
  <div
    style={{
      overflow: 'auto',
      padding: '15px 10px 60px 10px',
      background: '#ffffff',
      height: '100%',
    }}
  >
    {children}
  </div>
)

export default ContentLayout
